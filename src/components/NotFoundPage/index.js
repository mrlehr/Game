// == Import npm
import React from 'react';

import './style.scss';

const NotFoundPage = () => (
  <div className="notFound">
   <h1 className="notFoundTitle" > 404 La page est introuvable petit curieux </h1>
  </div>
);

// == Export
export default NotFoundPage;
